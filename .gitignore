# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Typescript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# next files
.next/

# Generated by Next on every dev/build, and NOT stable: `next dev` writes
# `./.next/dev/types/routes.d.ts` while `next build` writes
# `./.next/types/routes.d.ts`, so a tracked copy flips back and forth with
# whichever ran last and gets swept into unrelated commits (it moved in six of
# the last commits). Nothing here typechecks a fresh clone before a dev/build
# regenerates it, so ignoring it is free.
next-env.d.ts

# sanity dist
sanity/dist
public/sanity
*.tar*

# sanity schema
sanity/schema.js

# Mac files
.DS_Store
**/.DS_Store

# Yarn
yarn-error.log
yarn.lock
.pnp/
.pnp.js

# Yarn Integrity file
.yarn-integrity

# Secrets. The negation is load-bearing: `.env*` otherwise swallows the tracked
# template too, and a checkout with no .env.example gives a new developer
# nothing to copy.
.env*
!.env.example
.gstack/

# Generated by tsc --incremental; flaps on every build.
tsconfig.tsbuildinfo
